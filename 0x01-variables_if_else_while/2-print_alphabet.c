#include <stdio.h>

/**
* main - print alphabet in lower case a-z
* Return: Always 0 (Success)
*/

int main(void)
{
char alpha = 'a';

while (alpha <= 'z' )

{
putchar(alpha);
alpha++;
}

return (0);
}
